
  - Configured AWS CLI and verified access to AWS account.
  - Created a single Terraform configuration with two AWS providers for different regions.
  - Retrieved the latest Ubuntu AMIs for both regions using Terraform data sources.
  - Provisioned one Linux EC2 instance in each region using provider alias.
  - Applied the Terraform plan to create resources.
